# RLC_dac_study_flash

这是一个基于 **STM32F407** 的 RLC 电路参数测量项目，通过 DAC 输出激励信号，采集响应信号并进行算法分析，实现电阻、电感、电容的自动识别与参数计算。

---

## 📌 项目简介

本项目实现了一套完整的阻抗测量系统：
- 使用 **DAC + 运算放大器** 输出多频点正弦扫频信号（100Hz ~ 100kHz）
- 通过 **ADC** 采集激励与响应信号的幅值和相位差
- 利用 **FFT 算法** 提取基波分量，计算阻抗幅值与相位
- 根据阻抗特性自动识别负载类型（R / L / C）并计算参数值
- 支持 **TFT-LCD 实时显示**测量结果与扫频曲线
- 可通过 **USB 虚拟串口** 输出数据，配合上位机分析

---

## 📂 项目结构


---

## ⚙️ 核心功能说明

### 1. 多频点正弦扫频
- 使用 TIM + DAC 生成不同频率的正弦激励信号
- 扫频范围：100Hz ~ 100kHz，支持自定义频点配置
- 每个频点输出稳定后，采集多组数据求平均，提高精度

### 2. 信号采集与 FFT 分析
- 双通道 ADC 同步采集激励与响应信号
- 利用 CMSIS-DSP 库的 FFT 函数提取基波分量
- 计算信号幅值、相位差，进而得到阻抗的模和相位角

### 3. 负载类型识别与参数计算
- 根据阻抗相位角判断负载类型：
  - 相位 ≈ 0°：纯电阻 R
  - 相位 > 0°：感性负载 L，计算电感值
  - 相位 < 0°：容性负载 C，计算电容值
- 支持开路、短路故障检测与异常处理

### 4. 实时显示与数据交互
- TFT-LCD 实时显示当前频点、阻抗值、相位角和负载类型
- USB 虚拟串口输出原始数据与计算结果，支持上位机波形绘制
- 可扩展蓝牙/WiFi 模块实现无线数据传输

---

## 🛠️ 开发环境

- 主控芯片：STM32F407ZGT6
- 开发环境：Keil MDK-ARM 5.37
- 库版本：STM32Cube HAL 库 + CMSIS-DSP 库
- 调试工具：J-Link V11
- 系统支持：FreeRTOS（可选）

---

## 📈 后续优化方向

- 增加自动档位切换逻辑，提升宽范围测量精度
- 优化 FFT 窗函数与滤波算法，降低噪声干扰
- 增加温度补偿，提高电感/电容测量稳定性
- 移植到 STM32H7 平台，支持更高采样率和带宽

---

## 👨‍💻 作者信息

- 作者：xht
- 学校：湖南理工大学
- 项目背景：全国大学生电子设计竞赛备战项目

---
